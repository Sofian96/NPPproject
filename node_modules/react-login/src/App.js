import React, { Component } from 'react';

import Enter from './Container/Enter.js'
import {appInfo} from './service'
class App extends Component {
  constructor(props) {
    super(props)
  }

  render() {
    return (
      <div>
         <Enter registerUrl={`${appInfo.address.register}`} loginUrl={`${appInfo.address.login}`}/>
      </div>
    );
  }
}

export default App;
